#include <iostream>
using namespace std;
int main()
{
    int N, M, s, d;
    cin >> N >> M >> s >> d;
    int rescue[N];
    int road[N][N];
    for (int i = 0; i < N; i++)
        cin >> rescue[i];
    for (int i = 0; i < M; i++)
    {
        int c1, c2, l;
        road[c1][c2] = l;
    }

}